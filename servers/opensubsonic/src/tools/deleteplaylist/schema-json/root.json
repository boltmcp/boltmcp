{
  "type": "object",
  "properties": {
    "id": {
      "description": "ID of the playlist to delete, as obtained by `getPlaylists`.",
      "type": "string"
    }
  },
  "required": [
    "id"
  ]
}