import { z } from "zod"

export const inputParamsSchema = {
  "FileType": z.string().describe("The payment file type."),
  "FileHash": z.string().min(1).max(44).describe("A base64 encoding of a SHA256 hash of the file to be uploaded."),
  "FileReference": z.string().min(1).max(40).describe("Reference for the file.").optional(),
  "RequestedExecutionDateTime": z.string().datetime({ offset: true }).describe("The date and time when the initiating party requests the clearing agent to process the payment.\nThis is the date the debtor's account is to be debited.\n\nAll dates in the JSON payloads are represented in [ISO 8601](https://en.wikipedia.org/wiki/ISO_8601) date-time format.\nAll date-time fields in responses must include the timezone.\n\nFor example: `2017-04-05T10:43:07+00:00`.").optional(),
  "LocalInstrument": z.string().min(1).max(50).describe("User community specific instrument.\n\nUse this element to specify a local instrument, local clearing option and/or further qualify the service or service level.").optional(),
  "DebtorAccount": z.record(z.any()).describe("<llm-instruction>This part of the input schema is truncated. If you want to pass the property `DebtorAccount` to the tool, first call the tool `expandSchema` with \"/properties/Data/properties/Initiation/properties/DebtorAccount\" in the list of pointers. This will return the expanded input schema which you can then use in the tool call. You may have to call `expandSchema` multiple times if the schema is nested.</llm-instruction>\n<property-description>The details to identify the account.</property-description>").optional(),
  "RemittanceInformation": z.record(z.any()).describe("<llm-instruction>This part of the input schema is truncated. If you want to pass the property `RemittanceInformation` to the tool, first call the tool `expandSchema` with \"/properties/Data/properties/Initiation/properties/RemittanceInformation\" in the list of pointers. This will return the expanded input schema which you can then use in the tool call. You may have to call `expandSchema` multiple times if the schema is nested.</llm-instruction>\n<property-description>The information used for matching an entry with the items that the transfer is intended to settle, such as commercial invoices in an accounts' receivable system.</property-description>").optional()
}