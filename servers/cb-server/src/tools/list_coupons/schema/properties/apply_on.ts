import { z } from "zod"

export const inputParamsSchema = {
  "is": z.enum(["invoice_amount","each_specified_item"]).describe("\\* \\`invoice_amount\\` - The coupon is applied to the invoice `sub_total`. \\* \\`specified_items_total\\` - \\*\\*(Deprecated)\\*\\* Discount will be applied to the total of plan and addon items specified. \\* \\`each_specified_item\\` -  \nThe coupon is applied to the `invoice.line_item.amount` that corresponds to the item price specified by `item_price_id`.  \nThe coupon is applied to the `invoice.line_item.amount` that corresponds to the plan or addon specified by `plan_ids` and `addon_ids`. \\* \\`each_unit_of_specified_items\\` - \\*\\*(Deprecated)\\*\\* Discount will be applied to each unit of plan and addon items specified.\n").optional(),
  "is_not": z.enum(["invoice_amount","each_specified_item"]).describe("\\* \\`invoice_amount\\` - The coupon is applied to the invoice `sub_total`. \\* \\`specified_items_total\\` - \\*\\*(Deprecated)\\*\\* Discount will be applied to the total of plan and addon items specified. \\* \\`each_specified_item\\` -  \nThe coupon is applied to the `invoice.line_item.amount` that corresponds to the item price specified by `item_price_id`.  \nThe coupon is applied to the `invoice.line_item.amount` that corresponds to the plan or addon specified by `plan_ids` and `addon_ids`. \\* \\`each_unit_of_specified_items\\` - \\*\\*(Deprecated)\\*\\* Discount will be applied to each unit of plan and addon items specified.\n").optional(),
  "in": z.string().regex(new RegExp("^\\[(invoice_amount|specified_items_total|each_specified_item|each_unit_of_specified_items)(,(invoice_amount|specified_items_total|each_specified_item|each_unit_of_specified_items))*\\]$")).optional(),
  "not_in": z.string().regex(new RegExp("^\\[(invoice_amount|specified_items_total|each_specified_item|each_unit_of_specified_items)(,(invoice_amount|specified_items_total|each_specified_item|each_unit_of_specified_items))*\\]$")).optional()
}