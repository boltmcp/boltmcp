{
  "description": "<dfn class=\"text-muted\">optional, enumerated string filter</dfn><br>Filter subscription ramps based on <code>status</code>.<div class=\"alert alert-warning\"> <strong>Caution</strong> <ul> <li>The <code>subscription_id</code> filter must be passed when filtering by <code>status</code>.</li> <li><code>status</code> filter should not be passed when <code>include_deleted</code> is set to <code>true</code>.</li> </ul></div> Possible values are : <dfn>scheduled, succeeded, failed.</dfn><br><b>Supported operators : </b>is, in<br><br><b>Example <span class=\"cb-char-arrow\">→</span> </b><i>status[is] = \"SCHEDULED\"</i>",
  "type": "object",
  "properties": {
    "in": {
      "pattern": "^\\[(scheduled|succeeded|failed|draft)(,(scheduled|succeeded|failed|draft))*\\]$",
      "type": "string"
    },
    "is": {
      "type": "string",
      "description": "\\* \\`scheduled\\` - Status of the subscription schedule on creation. \\* \\`succeeded\\` - The execution status of the schedule if success. \\* \\`failed\\` - The execution status of the schedule if failed. \\* \\`draft\\` - Status of the subscription schedule considering as draft\n",
      "enum": [
        "scheduled",
        "succeeded",
        "failed",
        "draft"
      ]
    }
  },
  "example": "SCHEDULED",
  "deprecated": false
}