import { z } from "zod"

export const inputParamsSchema = {
  "is": z.enum(["sum_of_usages","last_usage","max_usage"]).describe("\\* \\`sum_of_usages\\` - the net quantity is the sum of the `quantity` of all usages for the current term. \\* \\`last_usage\\` - from among the usage records for the [item price](/docs/api/subscriptions?prod_cat_ver=2#subscription_subscription_items_item_price_id) with `usage_date` within the relevant billing period, the `quantity` of the usage record with the most recent `usage_date` is taken as the net quantity consumed. \\* \\`max_usage\\` - from among the usage records for the [item price](/docs/api/subscriptions?prod_cat_ver=2#subscription_subscription_items_item_price_id) with `usage_date` within the relevant billing period, the `quantity` of the usage record with the maximum value is taken as the net quantity consumed.\n").optional(),
  "is_not": z.enum(["sum_of_usages","last_usage","max_usage"]).describe("\\* \\`sum_of_usages\\` - the net quantity is the sum of the `quantity` of all usages for the current term. \\* \\`last_usage\\` - from among the usage records for the [item price](/docs/api/subscriptions?prod_cat_ver=2#subscription_subscription_items_item_price_id) with `usage_date` within the relevant billing period, the `quantity` of the usage record with the most recent `usage_date` is taken as the net quantity consumed. \\* \\`max_usage\\` - from among the usage records for the [item price](/docs/api/subscriptions?prod_cat_ver=2#subscription_subscription_items_item_price_id) with `usage_date` within the relevant billing period, the `quantity` of the usage record with the maximum value is taken as the net quantity consumed.\n").optional(),
  "in": z.string().regex(new RegExp("^\\[(sum_of_usages|last_usage|max_usage)(,(sum_of_usages|last_usage|max_usage))*\\]$")).optional(),
  "not_in": z.string().regex(new RegExp("^\\[(sum_of_usages|last_usage|max_usage)(,(sum_of_usages|last_usage|max_usage))*\\]$")).optional()
}