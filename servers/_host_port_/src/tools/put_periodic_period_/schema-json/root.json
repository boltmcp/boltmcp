{
  "type": "object",
  "properties": {
    "period": {
      "description": "The name of the period for which you would like to grab a periodic note.",
      "default": "daily",
      "enum": [
        "daily",
        "weekly",
        "monthly",
        "quarterly",
        "yearly"
      ],
      "type": "string"
    }
  },
  "required": [
    "period"
  ]
}