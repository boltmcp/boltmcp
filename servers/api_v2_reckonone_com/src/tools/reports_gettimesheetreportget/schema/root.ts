import { z } from "zod"

export const inputParamsSchema = {
  "bookId": z.string().describe("The book's id."),
  "parameters": z.object({ "token": z.string().nullable().describe("An authentication token. Note that this is not required if it is supplied via the HTTP `Authorize` header.").optional(), "period": z.enum(["AllDates","SpecifiedDates"]).describe("Periods that can be used for the Super Transactions report.").nullable().describe("The period."), "fromDate": z.string().datetime({ offset: true }).nullable().describe("The 'from' date for the report.\nOnly required if `period` is `SpecifiedDates`."), "toDate": z.string().datetime({ offset: true }).nullable().describe("The 'to' date for the report.\nOnly required if `period` is `SpecifiedDates`."), "groupBy": z.enum(["None","Employee","Project","Classification","Customer","ServiceItem","BillableStatus","TimesheetStatus","WeeklyPeriod"]).describe("Grouping option for timesheet report.").describe("The data grouping for the report."), "employees": z.array(z.string()).nullable().describe("A list of employee IDs to include in the report.\nIf omitted, all employees will be included."), "projects": z.array(z.string()).nullable().describe("A list of project IDs to include in the report.\nIf omitted, all projects will be included."), "classifications": z.array(z.string()).nullable().describe("A list of classification IDs to include in the report.\nIf omitted, all classifications will be included."), "customers": z.array(z.string()).nullable().describe("A list of customer IDs to include in the report.\nIf omitted, all customers will be included."), "serviceItems": z.array(z.string()).nullable().describe("A list of service item IDs to include in the report.\nIf omitted, all service items will be included."), "status": z.array(z.enum(["NonBillable","Billable","Invoiced","Billed"]).describe("Timesheet entry's billing status.")).nullable().describe("The billing statuses to include in the report.\nIf omitted, all billing statuses will be included."), "timesheetStatus": z.array(z.enum(["Draft","Submitted","Declined","Approved"]).describe("Timesheet status.")).nullable().describe("The timesheet statuses to include in the report.\nIf omitted, all timesheet statuses will be included."), "orientation": z.enum(["Portrait","Landscape"]).describe("The report orientation.").nullable().describe("The report's orientation."), "columns": z.array(z.enum(["Employee","Project","Classification","Customer","ServiceItem","BillableStatus","TimesheetStatus","Notes"]).describe("Timesheet Report columns.")).nullable().describe("A list of columns to include in the report.\nIf omitted, all columns will be returned.") }).strict().describe("The parameters for the timesheet report.").nullable().describe("The report parameters.").optional(),
  "format": z.string().nullable().describe("The format to use, e.g. pdf.").optional(),
  "download": z.boolean().describe("Whether the result is for download. If true, a ContentDisposition header will be added.").optional()
}