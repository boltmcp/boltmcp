{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "ledgerAccountId": {
      "description": "The ledger account's id.",
      "type": "string",
      "format": "guid"
    }
  },
  "required": [
    "bookId",
    "ledgerAccountId"
  ]
}