{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "baseDate": {
      "description": "The base date to use when checking for overdue bills.",
      "type": "string",
      "format": "date-time",
      "nullable": true
    }
  },
  "required": [
    "bookId"
  ]
}