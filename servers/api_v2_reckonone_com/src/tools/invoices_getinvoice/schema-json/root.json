{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "invoiceId": {
      "description": "The invoice's id.",
      "type": "string",
      "format": "guid"
    },
    "format": {
      "description": "The format to use, e.g. pdf.",
      "type": "string",
      "nullable": true
    }
  },
  "required": [
    "bookId",
    "invoiceId"
  ]
}