{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "invoiceId": {
      "description": "The invoice to get allocable transactions for.",
      "type": "string",
      "format": "guid"
    },
    "page": {
      "description": "The page to retrieve.",
      "type": "integer",
      "format": "int32",
      "default": 1
    },
    "perPage": {
      "description": "The number of allocable transactions per page.",
      "type": "integer",
      "format": "int32",
      "default": 0,
      "nullable": true
    }
  },
  "required": [
    "bookId",
    "invoiceId"
  ]
}