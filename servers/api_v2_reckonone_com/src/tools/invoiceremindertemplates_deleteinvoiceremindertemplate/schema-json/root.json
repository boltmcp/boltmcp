{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "templateId": {
      "description": "The invoice reminder template's id.",
      "type": "string",
      "format": "guid"
    }
  },
  "required": [
    "bookId",
    "templateId"
  ]
}