{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "creditNoteId": {
      "description": "The credit note's id.",
      "type": "string",
      "format": "guid"
    },
    "format": {
      "description": "The format to use, e.g. pdf.",
      "type": "string",
      "nullable": true
    }
  },
  "required": [
    "bookId",
    "creditNoteId"
  ]
}