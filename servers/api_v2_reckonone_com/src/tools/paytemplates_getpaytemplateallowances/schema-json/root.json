{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "payTemplateId": {
      "description": "The pay template's id.",
      "type": "string",
      "format": "guid"
    },
    "page": {
      "description": "The page to retrieve.",
      "type": "integer",
      "format": "int32",
      "default": 1
    },
    "perPage": {
      "description": "The number of pay template allowances per page.",
      "type": "integer",
      "format": "int32",
      "default": 0,
      "nullable": true
    }
  },
  "required": [
    "bookId",
    "payTemplateId"
  ]
}