{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "fromDate": {
      "description": "The date range start.",
      "type": "string",
      "format": "date-time"
    },
    "toDate": {
      "description": "The date range end.",
      "type": "string",
      "format": "date-time"
    },
    "page": {
      "description": "The page to retrieve.",
      "type": "integer",
      "format": "int32",
      "default": 1
    },
    "perPage": {
      "description": "The number of unlinked super transactions per page.",
      "type": "integer",
      "format": "int32",
      "default": 0,
      "nullable": true
    }
  },
  "required": [
    "bookId"
  ]
}