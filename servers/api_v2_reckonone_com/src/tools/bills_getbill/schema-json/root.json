{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "billId": {
      "description": "The bill's id.",
      "type": "string",
      "format": "guid"
    },
    "format": {
      "description": "The format to use, e.g. pdf.",
      "type": "string",
      "nullable": true
    }
  },
  "required": [
    "bookId",
    "billId"
  ]
}