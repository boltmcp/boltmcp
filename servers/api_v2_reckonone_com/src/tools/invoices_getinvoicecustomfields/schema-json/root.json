{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "invoiceId": {
      "description": "The invoice to get custom fields for.",
      "type": "string",
      "format": "guid"
    }
  },
  "required": [
    "bookId",
    "invoiceId"
  ]
}