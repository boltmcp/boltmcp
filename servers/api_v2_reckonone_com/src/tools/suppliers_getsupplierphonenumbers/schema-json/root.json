{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "supplierId": {
      "description": "The supplier's id.",
      "type": "string",
      "format": "guid"
    }
  },
  "required": [
    "bookId",
    "supplierId"
  ]
}