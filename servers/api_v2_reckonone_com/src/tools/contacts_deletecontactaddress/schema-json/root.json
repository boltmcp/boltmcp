{
  "type": "object",
  "properties": {
    "bookId": {
      "description": "The book's id.",
      "type": "string",
      "format": "guid"
    },
    "contactId": {
      "description": "The contact's id.",
      "type": "string",
      "format": "guid"
    },
    "type": {
      "description": "The address's type by name or id.",
      "type": "string"
    }
  },
  "required": [
    "bookId",
    "contactId",
    "type"
  ]
}