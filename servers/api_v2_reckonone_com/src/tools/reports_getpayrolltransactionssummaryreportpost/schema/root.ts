import { z } from "zod"

export const inputParamsSchema = {
  "bookId": z.string().describe("The book's id."),
  "parameters": z.object({ "token": z.string().nullable().describe("An authentication token. Note that this is not required if it is supplied via the HTTP `Authorize` header.").optional(), "period": z.enum(["AllDates","SpecifiedDates"]).describe("Periods that can be used for the Payroll Transactions Summary report.").nullable().describe("The period."), "fromDate": z.string().datetime({ offset: true }).nullable().describe("The 'from' date for the report.\nOnly required if `period` is `SpecifiedDates`."), "toDate": z.string().datetime({ offset: true }).nullable().describe("The 'to' date for the report.\nOnly required if `period` is `SpecifiedDates`."), "payruns": z.array(z.string()).nullable().describe("A list of pay run IDs to include in the report.\nIf omitted, all pay runs will be included."), "orientation": z.enum(["Portrait","Landscape"]).describe("The report orientation.").nullable().describe("The report's orientation.\nIgnored for JSON report format."), "columns": z.array(z.enum(["Code","Project","TaxCode"]).describe("Payroll Transactions Summary Report columns.")).nullable().describe("A list of columns to include in the report.\nIf omitted, all columns will be returned.") }).strict().describe("The parameters for the Payroll Transactions Summary report.").nullable().describe("The report parameters.").optional(),
  "format": z.string().nullable().describe("The format to use, e.g. pdf.").optional(),
  "download": z.boolean().describe("Whether the result is for download. If true, a ContentDisposition header will be added.").optional()
}