{
  "type": "object",
  "properties": {
    "page": {
      "description": "Page number",
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "title": "Page"
    },
    "per_page": {
      "description": "Items per page",
      "type": "integer",
      "maximum": 100,
      "minimum": 1,
      "default": 10,
      "title": "Per Page"
    },
    "machine_id": {
      "description": "Filter by machine UUID",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Machine Id"
    },
    "search": {
      "description": "Search by name",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Search"
    },
    "active_only": {
      "description": "Show only active tags",
      "type": "boolean",
      "default": true,
      "title": "Active Only"
    },
    "show_hidden": {
      "description": "Include hidden tags",
      "type": "boolean",
      "default": false,
      "title": "Show Hidden"
    }
  },
  "required": []
}