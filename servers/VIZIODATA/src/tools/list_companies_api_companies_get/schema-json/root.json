{
  "type": "object",
  "properties": {
    "page": {
      "description": "Page number",
      "type": "integer",
      "minimum": 1,
      "default": 1,
      "title": "Page"
    },
    "per_page": {
      "description": "Items per page",
      "type": "integer",
      "maximum": 100,
      "minimum": 1,
      "default": 10,
      "title": "Per Page"
    },
    "search": {
      "description": "Search by name or cnpj",
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Search"
    },
    "active_only": {
      "description": "Show only active companies",
      "type": "boolean",
      "default": true,
      "title": "Active Only"
    },
    "show_hidden": {
      "description": "Include hidden companies",
      "type": "boolean",
      "default": false,
      "title": "Show Hidden"
    }
  },
  "required": []
}