{
  "type": "object",
  "properties": {
    "business_id": {
      "type": "string",
      "format": "uuid",
      "title": "Business Id"
    },
    "sort_by": {
      "description": "Sort by options for packs for app.\n\n- `title`: sort by the title of the pack.\n- `join_date`: sort by when the user joined the pack.\n- `newest_first`: get the newest joined or applied packs first.",
      "default": "asc(title)",
      "type": "string",
      "enum": [
        "asc(title)",
        "desc(title)",
        "asc(join_date)",
        "desc(join_date)",
        "desc(newest_first)"
      ],
      "title": "AppPackByHubSortBy"
    },
    "limit": {
      "type": "integer",
      "maximum": 100,
      "exclusiveMinimum": 0,
      "default": 10,
      "title": "Limit"
    },
    "token": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "title": "Token"
    },
    "id": {
      "anyOf": [
        {
          "type": "string",
          "format": "uuid"
        },
        {
          "type": "null"
        }
      ],
      "title": "Id"
    },
    "search": {
      "description": "Search for the title of the Group.",
      "anyOf": [
        {
          "type": "string",
          "minLength": 1
        },
        {
          "type": "null"
        }
      ],
      "title": "Search"
    },
    "is_member": {
      "description": "To show groups where current user is or is not member",
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "type": "null"
        }
      ],
      "title": "Is Member"
    },
    "embed_treet_count": {
      "type": "boolean",
      "default": false,
      "title": "Embed Treet Count"
    },
    "embed_treet_owner_avatars": {
      "type": "boolean",
      "default": false,
      "title": "Embed Treet Owner Avatars"
    },
    "embed_featured_treets": {
      "type": "boolean",
      "default": false,
      "title": "Embed Featured Treets"
    }
  },
  "required": [
    "business_id"
  ]
}