{
  "type": "object",
  "properties": {
    "integration": {
      "description": "Type of OAuth integration.\n\nNOTE: remember to update migrations if you update this enum.",
      "type": "string",
      "enum": [
        "planday",
        "google"
      ],
      "title": "OauthIntegration"
    },
    "code": {
      "type": "string",
      "title": "Code"
    },
    "state": {
      "type": "string",
      "title": "State"
    }
  },
  "required": [
    "integration",
    "code",
    "state"
  ]
}